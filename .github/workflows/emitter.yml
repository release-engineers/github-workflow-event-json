name: Event Emitter

on:
  schedule:
    # “At 00:00 on Sunday.” t. crontab.guru
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "OK"
  call-workflow:
    uses: release-engineers/github-workflow-event-json/.github/workflows/sink.yml@master
    with:
      sample_input_call_1: "789"
